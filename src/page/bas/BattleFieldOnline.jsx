import React from "react";
import Bas from "./bas.jsx"; // ตรวจสอบ path ให้ถูก
import "./style.css"; // ตรวจสอบ path ให้ถูก

function BattleFieldOnline({ socket, roomId, myRole }) {
  // คำนวณว่าใครคือศัตรูของเรา
  // ถ้าเราเป็น P1 ศัตรูคือ P2, ถ้าเราเป็น P2 ศัตรูคือ P1
  const enemyRole = myRole === "P1" ? "P2" : "P1";

  if (!myRole) return <div>กำลังรอระบุตัวตน...</div>;

  return (
    <div className="battle-wrapper" style={{ flexDirection: 'column', height: '100vh' }}>
      
      {/* ------------------------------------------- */}
      {/* 🛑 โซนกระดานศัตรู (อยู่ด้านบน) */}
      {/* ต้องใส่ playerId เป็นของศัตรู เพื่อให้มันรอรับ Socket ของศัตรู */}
      {/* ------------------------------------------- */}
      <div className="enemy-side" style={{ transform: 'rotate(180deg)', borderBottom: '2px solid red', paddingBottom: '10px' }}>
         <Bas 
            playerId={enemyRole}   // 👈 สำคัญมาก! ต้องใส่ ID ของศัตรู (เช่น P2)
            socket={socket}        // 👈 ต้องส่ง socket เข้าไป
            roomId={roomId}
            isEnemy={true}         // บอกว่าเป็นศัตรู (จะโชว์หลังการ์ด)
         />
      </div>

      {/* ------------------------------------------- */}
      {/* ✅ โซนกระดานของเรา (อยู่ด้านล่าง) */}
      {/* ------------------------------------------- */}
      <div className="my-side" style={{ paddingTop: '10px' }}>
         <Bas 
            playerId={myRole}      // 👈 ใส่ ID ของเรา (เช่น P1)
            socket={socket}
            roomId={roomId}
            isEnemy={false}        // บอกว่าเป็นเรา (โชว์การ์ดจริง + กดได้)
         />
      </div>

    </div>
  );
}

export default BattleFieldOnline;